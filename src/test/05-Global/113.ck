// comma-separated declarations; references; global
global Gain h, g;

// NOTE: null references doesn't seem to be possible for global
// global Gain @ h, g;

// print
<<< "success" >>>;

// 1.4.1.2 (ge) | added fall 2022
