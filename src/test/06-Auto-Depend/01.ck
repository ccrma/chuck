// class definition
// desired behavior: ChucK would automatically look for Bar.ck in the
// “classpath” and compile it, and then resolve the Bar in 01.ck

class Foo
{
     Bar bar;
}

<<<"success">>>;
